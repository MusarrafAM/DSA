//! ~n – Bitwise NOT Operator  (0 into 1 , 1 into 0)
//! >>> operator converts the signed 32-bit integer to an unsigned 32-bit integer.
// this is just a trick to do this. remember this.

function flippingBits(n) {
  // Write your code here
  return ~n >>> 0;
}
